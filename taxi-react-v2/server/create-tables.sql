create table adresses ( id int(11) not null auto_increment,
       phone varchar(25) not null,
       name varchar(255),
       adress varchar(255),
       entrance varchar(50),
       comment varchar(255),
       PRIMARY KEY(id) );

